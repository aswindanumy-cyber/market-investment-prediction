services:
  predict:
    build: .
    command: sh loop.sh
    volumes:
      - .:/app
    environment:
      - MPLBACKEND=Agg
      - ALPHAVANTAGE_API_KEY=${ALPHAVANTAGE_API_KEY:-}
    restart: unless-stopped
